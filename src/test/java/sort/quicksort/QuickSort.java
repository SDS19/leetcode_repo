package sort.quicksort;

/**
 * Arrays.class contains various methods for manipulating arrays (sort und search)
 *
 * Dual-Pivot Quicksort offers O(nlogn) performance on all data sets,
 * and is typically faster than traditional (one-pivot) Quicksort
 * implementations
 *
 * There are also additional algorithms, invoked from the Dual-Pivot
 * Quicksort, such as mixed insertion sort, merging of runs and heap
 * sort, counting sort and parallel merge sort.
 */
public class QuickSort {
}
